from pathlib import Path


def preprocess_file(path):
    """Placeholder file preprocessing that currently returns the original path."""
    return str(Path(path))


def preprocess_json(data):
    """Placeholder JSON preprocessing that currently returns the original payload."""
    return data

